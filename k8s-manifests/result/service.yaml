# Result Service
apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: voting
spec:
  selector:
    app: result
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
