# Redis development environment overrides

redis:
  architecture: standalone
  
  master:
    persistence:
      enabled: false  # No persistence needed in dev
      size: 1Gi
    
    resources:
      requests:
        memory: 64Mi
        cpu: 50m
      limits:
        memory: 128Mi
        cpu: 100m
