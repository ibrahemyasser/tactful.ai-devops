{{- if and .Values.result.enabled .Values.result.sidecar.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: result-nginx-config
  namespace: {{ .Values.global.namespace }}
  labels:
    app: result
    {{ include "vote-app.labels" . | nindent 4 }}
data:
  nginx.conf: |
    user nginx;
    worker_processes auto;
    error_log /dev/stderr warn;
    pid /tmp/nginx.pid;
    
    events {
      worker_connections 1024;
    }
    
    http {
      client_body_temp_path /tmp/client_temp;
      proxy_temp_path       /tmp/proxy_temp_path;
      fastcgi_temp_path     /tmp/fastcgi_temp;
      uwsgi_temp_path       /tmp/uwsgi_temp;
      scgi_temp_path        /tmp/scgi_temp;
      
      access_log /dev/stdout;
      
      server {
        listen {{ .Values.result.sidecar.port }};
        
        # Strip /result prefix and proxy to app
        location /result/ {
          proxy_pass http://localhost:{{ .Values.result.sidecar.appPort }}/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
          # WebSocket support for Socket.IO
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection "upgrade";
        }
        
        location = /result {
          return 301 /result/;
        }
        
        # Health check
        location /health {
          return 200 "OK";
          add_header Content-Type text/plain;
        }
      }
    }
{{- end }}
